# To start a simulation with given configuration


def start(config):
    network = init(conf1)
    result = run(network, conf2)

    exp1(result)
    exp2(result)

start(network_size ) for ns in [...]
